---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cnpg-system
resources:
  - ../base/
patches:
  - path: ./deployment-image-patch.yaml
secretGenerator:
  - name: plugin-pgbackrest
    literals:
      - SIDECAR_IMAGE=registry.hub.docker.com/dalibo/cnpg-pgbackrest-sidecar:0.0.1a
